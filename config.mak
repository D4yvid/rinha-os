COMPILER_TARGET = i386-elf

CC = $(COMPILER_TARGET)-gcc
LD = $(COMPILER_TARGET)-ld
AR = $(COMPILER_TARGET)-ar
AS = $(COMPILER_TARGET)-as

OBJDUMP = $(COMPILER_TARGET)-objdump
OBJCOPY = $(COMPILER_TARGET)-objcopy

